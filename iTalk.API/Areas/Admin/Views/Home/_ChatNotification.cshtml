@*<div id="notify-modal" class="modal fade" data-backdrop="false">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div layout>
                        <img ng-src="{{users[notifications[0].senderId.toString()].thumbnail}}" src="~/Img/portrait.png" class="img-rounded portrait-md" />
                        <span ng-bind="notifications[0].content"></span>
                    </div>
                </div>
            </div>
        </div>
    </div>*@
<md-toast class="notify-toast">
    <div layout>
        <img thumbnail="user.thumbnail" class="" />
        <div flex="100" layout-margin>
            <h4 ng-if="!group" ng-bind="user.alias"></h4>
            <h4 ng-if="group">{{user.alias}} [{{group.name}}]</h4>
            <span class="text-muted" ng-bind="chat.content"></span>
        </div>
        <div>
            <button type="button" class="close pull-right" aria-label="Close" ng-click="closeNotification()">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    </div>
    @*<button type="button" class="close pull-right" aria-label="Close" ng-click="closeNotification()">
            <span aria-hidden="true">&times;</span>
        </button>
        <md-list-item class="md-2-line">
            <img thumbnail="getUser(notifications[0].senderId).thumbnail" class="portrait-md" />
            <div class="md-list-item-text">
                <h3 ng-bind="getUser(notifications[0].senderId).alias"></h3>
                <span class="text-muted" ng-bind="notifications[0].content"></span>
            </div>
        </md-list-item>*@
</md-toast>
